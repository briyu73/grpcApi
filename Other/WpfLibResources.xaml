<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

  <ResourceDictionary.MergedDictionaries>
    <!-- Material Design base themes -->
    <!-- Important: Do not include the BaseTheme here or it will break the theme switching functionality -->
    <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <FontFamily x:Key="LatoBold">pack://application;,,,/Fonts/#Lato bold</FontFamily>

  <!-- This is the main background colour of the application -->
  <sys:Double x:Key="AppFontSize">14</sys:Double>
  <FontFamily x:Key="AppFontFamily">MS Reference Sans Serif</FontFamily>
  <FontWeight x:Key="AppFontWeight">Medium</FontWeight>

  <!-- All application window style -->
  <Style x:Key="{x:Type Window}" TargetType="{x:Type Window}">
    <Setter Property="TextElement.Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Background}"/>
    <Setter Property="TextElement.FontWeight" Value="{StaticResource AppFontWeight}"/>
    <Setter Property="TextElement.FontSize" Value="{StaticResource AppFontSize}"/>
    <Setter Property="FontFamily" Value="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"/>
    <Setter Property="UseLayoutRounding" Value="True" />
  </Style>

  <!-- All other views style -->
  <Style x:Key="BamViewStyle" TargetType="{x:Type UserControl}">
    <Setter Property="TextElement.Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Background}"/>
    <Setter Property="TextElement.FontWeight" Value="{StaticResource AppFontWeight}"/>
    <Setter Property="TextElement.FontSize" Value="{StaticResource AppFontSize}"/>
    <Setter Property="FontFamily" Value="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"/>
    <Setter Property="UseLayoutRounding" Value="True" />
  </Style>

  <Style x:Key="fontAwesomeBtnIconStyle" TargetType="fa:IconImage">
    <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
    <Setter Property="Width" Value="16" />
    <Setter Property="Height" Value="16" />
  </Style>

  <Style x:Key="fontAwesomeIconStyle" TargetType="fa:IconImage">
    <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
    <Setter Property="Width" Value="16" />
    <Setter Property="Height" Value="16" />
  </Style>

  <Style TargetType="{x:Type GroupBox}" BasedOn="{StaticResource MaterialDesignGroupBox}">
    <Setter Property="Margin" Value="0,8,0,0" />
    <Setter Property="Padding" Value="16" />
    <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesign.Brush.Divider}" />
    <Setter Property="BorderThickness" Value="3" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type GroupBox}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="12"
                  Padding="0"
                  SnapsToDevicePixels="True">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <Border Grid.Row="0"
                      Background="{DynamicResource MaterialDesign.Brush.Paper}"
                      CornerRadius="12,12,0,0"
                      Padding="16,10,16,2">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  TextElement.FontSize="15"
                                  TextElement.FontWeight="SemiBold"
                                  TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}" />
              </Border>

              <ContentPresenter Grid.Row="1"
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  
</ResourceDictionary>