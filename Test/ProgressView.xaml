<!-- Classification: OFFICIAL
   - 
   - Copyright (C) 2021 Commonwealth of Australia.
   - 
   - All rights reserved.
   - 
   - The copyright herein resides with the Commonwealth of Australia.
   - The material(s) may not be used, modified, copied and/or distributed
   - without the written permission of the Commonwealth of Australia
   - represented by Defence Science and Technology Group, the Department
   - of Defence. The copyright notice above does not evidence any actual or 
   - intended publication of such material(s).
   - 
   - This material is provided on an "AS IS" basis and the Commonwealth of
   - Australia makes no representation or warranties of any kind, express 
   - or implied, of merchantability or fitness for any purpose. The
   - Commonwealth of Australia does not accept any liability arising from or
   - connected to the use of the material.
   - 
   - Use of the material is entirely at the Licensee's own risk. -->

<UserControl x:Class="FlewsApp.Modules.UI.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sf="http://swordfish.com.au/WPF/1.0">

  <UserControl.Resources>
    <Style x:Key="RoundCorner" TargetType="{x:Type Button}">
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Padding" Value="1"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Grid x:Name="grid">
              <Border x:Name="border" CornerRadius="9" BorderBrush="Black" BorderThickness="2">
                <Border.Background>
                  <RadialGradientBrush GradientOrigin="0.496,1.052">
                    <RadialGradientBrush.RelativeTransform>
                      <TransformGroup>
                        <ScaleTransform CenterX="0.5" CenterY="0.5" 
                                                        ScaleX="1.5" ScaleY="1.5"/>
                        <TranslateTransform X="0.02" Y="0.3"/>
                      </TransformGroup>
                    </RadialGradientBrush.RelativeTransform>
                    <GradientStop Offset="1" Color="#00000000"/>
                    <GradientStop Offset="0.3" Color="#FFFFFFFF"/>
                  </RadialGradientBrush>
                </Border.Background>
                <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.FontWeight="Bold">
                </ContentPresenter>
              </Border>

            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" TargetName="border">
                  <Setter.Value>
                    <RadialGradientBrush GradientOrigin="0.496,1.052">
                      <RadialGradientBrush.RelativeTransform>
                        <TransformGroup>
                          <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="1.5" ScaleY="1.5"/>
                          <TranslateTransform X="0.02" Y="0.3"/>
                        </TransformGroup>
                      </RadialGradientBrush.RelativeTransform>
                      <GradientStop Color="#00000000" Offset="1"/>
                      <GradientStop Color="#FF303030" Offset="0.3"/>
                    </RadialGradientBrush>
                  </Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" TargetName="border" Value="#FF33962B"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" TargetName="grid" Value="0.25"/>
              </Trigger>

            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="MLProgressBar" TargetType="ProgressBar">
      <Style.Triggers>
        <DataTrigger Binding="{Binding MultilineMode}" Value="True">
          <Setter Property="Height" Value="40"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding MultilineMode}" Value="False">
          <Setter Property="Height" Value="28"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="MLTextBlock" TargetType="TextBlock">
      <Style.Triggers>
        <DataTrigger Binding="{Binding MultilineMode}" Value="True">
          <Setter Property="HorizontalAlignment" Value="Left"/>
          <Setter Property="VerticalAlignment" Value="Top"/>
          <Setter Property="Padding" Value="5,2"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding MultilineMode}" Value="False">
          <Setter Property="HorizontalAlignment" Value="Center"/>
          <Setter Property="VerticalAlignment" Value="Center"/>
          <Setter Property="Padding" Value="5,0"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ProgressBar Width="350"
                 Foreground="ForestGreen"
                 Background="LightGray"
                 IsIndeterminate="{Binding Indeterminate}"
                 Minimum="0"
                 Maximum="100"
                 Value="{Binding Percentage}"
                 Style="{StaticResource MLProgressBar}"/>
    <TextBlock x:Name="messageTB" MaxWidth="350" MaxHeight="40" TextTrimming="None"
               FontWeight="Bold"
               Text="{Binding DisplayText}"
               Style="{StaticResource MLTextBlock}"/>
    <Button Grid.Row="1" Margin="4" HorizontalAlignment="Center" Height="25" Width="100"
            Visibility="{Binding Worker.WorkerSupportsCancellation, Converter={sf:BoolToVisibilityConverter}}"
            Style="{StaticResource RoundCorner}"
            Content="Stop" 
            Command="{Binding CancelProcessCommand}"/>
  </Grid>
</UserControl>
