<!-- Classification: OFFICIAL
   - 
   - Copyright (C) 2025 Commonwealth of Australia.
   - 
   - All rights reserved.
   - 
   - The copyright herein resides with the Commonwealth of Australia.
   - The material(s) may not be used, modified, copied and/or distributed
   - without the written permission of the Commonwealth of Australia
   - represented by Defence Science and Technology Group, the Department
   - of Defence. The copyright notice above does not evidence any actual or 
   - intended publication of such material(s).
   - 
   - This material is provided on an "AS IS" basis and the Commonwealth of
   - Australia makes no representation or warranties of any kind, express 
   - or implied, of merchantability or fitness for any purpose. The
   - Commonwealth of Australia does not accept any liability arising from or
   - connected to the use of the material.
   - 
   - Use of the material is entirely at the Licensee's own risk. -->

<UserControl x:Class="FlewsApp.Modules.DBManager.UI.Views.EFDataBaseViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FlewsApp.Modules.DBManager.UI.ViewModels"
             FontFamily="{DynamicResource AppFontFamily}" FontSize="{DynamicResource AppFontSize}"
             Background="WhiteSmoke">
  
  <prism:Dialog.WindowStyle>
    <Style TargetType="Window">
      <Setter Property="Width" Value="1250"/>
      <Setter Property="Height" Value="1000"/>
      <Setter Property="SizeToContent" Value="Manual"/>
      <Setter Property="ResizeMode" Value="CanResize"/>
    </Style>
  </prism:Dialog.WindowStyle>
  
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FlewsApp.Modules.UI;component/Resources/FlewsResourceDictionary.xaml" />
      </ResourceDictionary.MergedDictionaries>

      <!-- DataTemplate for primitive types (string, int, bool, etc.) -->
      <DataTemplate x:Key="PrimitiveTemplate">
        <Border Background="LightBlue" Padding="5" Margin="2" CornerRadius="3">
          <TextBlock Text="{Binding}" FontWeight="SemiBold"/>
        </Border>
      </DataTemplate>

      <!-- DataTemplate for DateTime -->
      <DataTemplate x:Key="DateTimeTemplate">
        <Border Background="LightGreen" Padding="5" Margin="2" CornerRadius="3">
          <TextBlock Text="{Binding StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" FontStyle="Italic"/>
        </Border>
      </DataTemplate>

      <!-- DataTemplate for Guid -->
      <DataTemplate x:Key="GuidTemplate">
        <Border Background="LightYellow" Padding="5" Margin="2" CornerRadius="3">
          <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="10"/>
        </Border>
      </DataTemplate>

      <!-- DataTemplate for null values -->
      <DataTemplate x:Key="NullTemplate">
        <Border Background="LightGray" Padding="5" Margin="2" CornerRadius="3">
          <TextBlock Text="[NULL]" FontStyle="Italic" Foreground="Gray"/>
        </Border>
      </DataTemplate>

      <!-- DataTemplate for complex objects -->
      <DataTemplate x:Key="ComplexObjectTemplate">
        <Border Background="DarkGray" Padding="5" Margin="2" CornerRadius="3">
          <Expander Header="{Binding TypeName}" IsExpanded="False">
            <ItemsControl ItemsSource="{Binding Properties}" Margin="10,5">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Width="150"/>
                    <TextBlock Text=": " FontWeight="Bold"/>
                    <ContentPresenter Content="{Binding Value}" 
                                    ContentTemplateSelector="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DataTemplateSelector}"/>
                  </StackPanel>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Expander>
        </Border>
      </DataTemplate>

      <!-- DataTemplate for collections -->
      <DataTemplate x:Key="CollectionTemplate">
        <Border Background="Lavender" Padding="5" Margin="2" CornerRadius="3">
          <Expander Header="{Binding Header}" IsExpanded="False">
            <ItemsControl ItemsSource="{Binding Items}" Margin="10,5">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <ContentPresenter Content="{Binding}" 
                                  ContentTemplateSelector="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DataTemplateSelector}"
                                  Margin="0,2"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Expander>
        </Border>
      </DataTemplate>
    </ResourceDictionary>
  </UserControl.Resources>
  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" Background="DarkSlateGray" Padding="10">
      <StackPanel Orientation="Horizontal">
        <Button Content="Load Database" Command="{Binding LoadDatabaseCommand}" 
                Padding="10,5" Margin="0,0,10,0" IsDefault="True"/>
        <TextBlock Text="{Binding DatabaseInfo}" Foreground="White" 
                          VerticalAlignment="Center" FontSize="14"/>
      </StackPanel>
    </Border>

    <!-- Main Content -->
    <Grid Grid.Row="1" MinWidth="1250" MinHeight="800">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="250"/>
        <ColumnDefinition Width="5"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <!-- Left Panel - Entity Types -->
      <Border Grid.Column="0" Background="WhiteSmoke" BorderBrush="Gray" BorderThickness="0,0,1,0">
        <DockPanel>
          <TextBlock Text="Entity Types" FontWeight="Bold" FontSize="16" 
                              Padding="10" Background="LightGray" DockPanel.Dock="Top"/>
          <ListBox ItemsSource="{Binding EntityTypes}" 
                            SelectedItem="{Binding SelectedEntityType}"
                            DisplayMemberPath="Name" Padding="5"/>
        </DockPanel>
      </Border>

      <!-- Splitter -->
      <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Gray"/>

      <!-- Right Panel - Entity Data -->
      <Border Grid.Column="2" Background="White">
        <DockPanel>
          <Grid DockPanel.Dock="Top" Background="LightGray">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Entities: " FontWeight="Bold" VerticalAlignment="Center" Margin="5,5,0,5"/>
            <TextBlock Grid.Column="1" Text="{Binding EntityCount, FallbackValue=0}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,5" />
            <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshEntitiesCommand}" 
                               Margin="20,5,5,5" Padding="8,2"/>
          </Grid>

          <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                                 VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Entities}" Margin="10">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border BorderBrush="DarkGray" BorderThickness="1" 
                                           Margin="0,5" CornerRadius="5" Padding="10">
                    <ContentPresenter Content="{Binding}" 
                                                        ContentTemplateSelector="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DataTemplateSelector}"/>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </DockPanel>
      </Border>
    </Grid>

    <!-- Status Bar -->
    <StatusBar Grid.Row="2">
      <StatusBarItem>
        <TextBlock Text="{Binding StatusMessage}"/>
      </StatusBarItem>
    </StatusBar>
  </Grid>
</UserControl>
